{
  "name": "@skippy/runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "bin": {
    "skippy-runner": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@skippy/gcal-write-mcp": "workspace:*",
    "convex": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
